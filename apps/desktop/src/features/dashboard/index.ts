/**
 * The dashboard's public surface. Outside code imports from here, never from a path inside the
 * folder — see `README.md` for the one sanctioned exception (`stores/dashboard.store`).
 */

export { DashboardPage } from './DashboardPage'
